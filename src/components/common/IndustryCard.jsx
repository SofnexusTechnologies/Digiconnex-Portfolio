import { ArrowRight } from "lucide-react";
import { Link } from "react-router-dom";

const IndustryCard = ({
  icon: Icon,
  title,
  description,
  services = [],
  link = "#",
  className = "",
}) => {
  return <div></div>;
};

export default IndustryCard;
